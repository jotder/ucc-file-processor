# Services

The `src/app/inspecto/api/` layer: one root-injectable service per backend resource, shared HTTP helpers,
and the global interceptors.

# Reference

* [API services](api-services.md) - the full resource-service catalog, the `api-base` helpers, interceptors, and shared async utilities.
