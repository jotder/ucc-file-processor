/**
 * RING 2 — the golden-eval harness: {@code EvalCase}/{@code Expect}, {@code FakeModelProvider},
 * {@code EvalRunner}, {@code EvalReport}, {@code EvalCaseLoader} (classpath JSON fixtures), and
 * JUnit glue ({@code Evals.asTests}). Deterministic; no Ollama in CI.
 *
 * <p>Implemented in K1.
 */
package com.gamma.agent.kernel.eval;
